namespace LoggerLib
{
    public enum LogLevel
    {
        TRACE,
        DEBUG,
        INFO,
        WARNING,
        ERROR
    }

    public enum LogOutput
    {
        CONSOLE,
        FILE,
        BOTH
    }
}
